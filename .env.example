# Copy to .env and fill in. scripts/setup.sh loads this and stores the keys in your Keychain.
# .env is gitignored; the app never reads it directly.

# Required. Chooses the next on-screen action.
TYPESAFE_API_KEY=

# Optional. Turns one spoken sentence into ordered steps.
OPENROUTER_API_KEY=
